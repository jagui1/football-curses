# Football Curses
Full stack learning project using where you will be able to request a curse from a verified witch
Mongo
Express.js
React.js 16
Node.js

## Development server

Run `npm i` to install dependencies.

Run `npm start` for a dev server.

Navigate to `http://localhost:3000/` for the main page.
